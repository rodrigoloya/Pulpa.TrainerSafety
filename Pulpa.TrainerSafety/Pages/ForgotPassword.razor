@page "/forgot-password"
@using Pulpa.TrainerSafety.Models
@inject NavigationManager Navigation
@inject HttpClient Http

<PageTitle>Recuperar Contraseña - Phishing Trainer</PageTitle>

<link href="css/auth-forms.css" rel="stylesheet" />

<div class="auth-container">
    <div class="auth-card">
        <div class="auth-header">
            <div class="auth-logo">
                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 7a2 2 0 012 2m4 0a6 6 0 01-7.743 5.743L11 17H9v2H7v2H4a1 1 0 01-1-1v-2.586a1 1 0 01.293-.707l5.964-5.964A6 6 0 1121 9z" />
                </svg>
            </div>
            <h1 class="auth-title">¿Olvidaste tu contraseña?</h1>
            <p class="auth-subtitle">
                @if (!emailSent)
                {
                    <text>No te preocupes, te enviaremos instrucciones para recuperarla</text>
                }
                else
                {
                    <text>Revisa tu bandeja de entrada</text>
                }
            </p>
        </div>

        @if (emailSent)
        {
            <div class="success-message" style="margin-bottom: 1.5rem;">
                <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 19v-8.93a2 2 0 01.89-1.664l7-4.666a2 2 0 012.22 0l7 4.666A2 2 0 0121 10.07V19M3 19a2 2 0 002 2h14a2 2 0 002-2M3 19l6.75-4.5M21 19l-6.75-4.5M3 10l6.75 4.5M21 10l-6.75 4.5m0 0l-1.14.76a2 2 0 01-2.22 0l-1.14-.76" />
                </svg>
                <div>
                    <strong>? Correo enviado exitosamente</strong>
                    <p style="margin: 0.25rem 0 0 0; font-size: 0.875rem;">
                        Hemos enviado un enlace de recuperación a <strong>@model.Email</strong>. 
                        Por favor, revisa tu bandeja de entrada y sigue las instrucciones.
                    </p>
                </div>
            </div>

            <div style="padding: 1rem; background-color: var(--background-secondary); border-radius: 0.5rem; margin-bottom: 1.5rem;">
                <p style="margin: 0; font-size: 0.875rem; color: var(--text-secondary);">
                    <strong>?? Consejo:</strong> Si no ves el correo en unos minutos, revisa tu carpeta de spam o correo no deseado.
                </p>
            </div>

            <div style="display: flex; flex-direction: column; gap: 0.75rem;">
                <button type="button" 
                        class="btn-primary"
                        @onclick="ResetForm">
                    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15" />
                    </svg>
                    <span>Enviar otro correo</span>
                </button>

                <a href="/login" 
                   style="display: inline-flex; align-items: center; justify-content: center; gap: 0.5rem; padding: 0.875rem 1.5rem; font-size: 1rem; font-weight: 600; color: var(--primary-color); background: var(--background-secondary); border: 2px solid var(--border-color); border-radius: 0.5rem; text-decoration: none; transition: all 0.2s ease;">
                    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
                    </svg>
                    <span>Volver al inicio de sesión</span>
                </a>
            </div>
        }
        else
        {
            @if (!string.IsNullOrEmpty(errorMessage))
            {
                <div class="validation-summary">
                    <strong>?? Error:</strong>
                    <p>@errorMessage</p>
                </div>
            }

            <EditForm Model="@model" OnValidSubmit="HandleForgotPassword" class="auth-form">
                <DataAnnotationsValidator />

                <div class="form-group">
                    <label class="form-label">
                        Correo electrónico <span class="required">*</span>
                    </label>
                    <div class="form-input-wrapper has-icon">
                        <span class="form-input-icon">
                            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                            </svg>
                        </span>
                        <InputText @bind-Value="model.Email" 
                                   type="email" 
                                   class="form-input" 
                                   placeholder="tu@email.com"
                                   disabled="@isSubmitting"
                                   autocomplete="email" />
                    </div>
                    <ValidationMessage For="@(() => model.Email)" />
                </div>

                <button type="submit" 
                        class="btn-primary @(isSubmitting ? "btn-loading" : "")" 
                        disabled="@isSubmitting">
                    @if (isSubmitting)
                    {
                        <span class="spinner"></span>
                        <span>Enviando...</span>
                    }
                    else
                    {
                        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                        </svg>
                        <span>Enviar enlace de recuperación</span>
                    }
                </button>
            </EditForm>

            <div class="auth-links">
                <div class="auth-link">
                    <a href="/login">
                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="none" viewBox="0 0 24 24" stroke="currentColor" style="display: inline; vertical-align: middle; margin-right: 0.25rem;">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
                        </svg>
                        Volver al inicio de sesión
                    </a>
                </div>
                <div class="auth-divider">
                    <span>o</span>
                </div>
                <div class="auth-link">
                    ¿No tienes una cuenta? <a href="/register">Regístrate gratis</a>
                </div>
            </div>
        }
    </div>
</div>

@code {
    private ForgotPasswordViewModel model = new();
    private bool isSubmitting = false;
    private bool emailSent = false;
    private string errorMessage = string.Empty;

    private async Task HandleForgotPassword()
    {
        try
        {
            isSubmitting = true;
            errorMessage = string.Empty;

            // Aquí llamarías a tu API para enviar el correo de recuperación
            // var response = await Http.PostAsJsonAsync("api/auth/forgot-password", model);
            
            // Simulación de llamada a la API
            await Task.Delay(1500);

            // if (response.IsSuccessStatusCode)
            // {
            //     emailSent = true;
            // }
            // else
            // {
            //     if (response.StatusCode == System.Net.HttpStatusCode.NotFound)
            //     {
            //         errorMessage = "No encontramos una cuenta asociada a este correo electrónico.";
            //     }
            //     else
            //     {
            //         var error = await response.Content.ReadFromJsonAsync<ErrorResponse>();
            //         errorMessage = error?.Message ?? "Error al enviar el correo. Por favor, intenta nuevamente.";
            //     }
            // }

            // Por ahora, simulamos éxito
            emailSent = true;
        }
        catch (HttpRequestException)
        {
            errorMessage = "Error de conexión. Por favor, verifica tu conexión a internet e intenta nuevamente.";
        }
        catch (Exception)
        {
            errorMessage = "Error inesperado. Por favor, intenta nuevamente más tarde.";
        }
        finally
        {
            isSubmitting = false;
        }
    }

    private void ResetForm()
    {
        emailSent = false;
        model = new ForgotPasswordViewModel();
        errorMessage = string.Empty;
    }
}
